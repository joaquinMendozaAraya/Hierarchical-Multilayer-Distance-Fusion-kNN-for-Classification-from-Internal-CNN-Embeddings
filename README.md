# Hierarchical-Multilayer-Distance-Fusion-kNN-for-Classification-from-Internal-CNN-Embeddings
HMDF-kNN is a post-hoc method for brain tumor MRI classification that fuses distances from internal CNN embeddings. It selects layer views, fusion weights, and kNN settings using validation data, then evaluates a frozen configuration on test data.

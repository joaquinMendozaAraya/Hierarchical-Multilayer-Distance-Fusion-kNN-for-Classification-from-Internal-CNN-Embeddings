# Release validation

Date: 2026-06-12

## Automated checks

- `pytest -q`: 1 passed.
- Friendly notebook: all seven code cells executed; no errors.
- Friendly notebook validation candidates: 185.
- Friendly notebook candidate table contains no `test_*` columns.
- No personal Windows, Colab, or Google Drive paths remain in release code or
  public method documentation.
- No file in the release exceeds 10 MB.

## Equivalence check against the audited implementation

The readable implementation in `models/hmdf_knn.py` was executed on the real
ScienceDB 3C / ResNet-18 embedding profile and compared with the frozen
selected result generated by the paper runner.

The following fields matched exactly:

- selected layer indices;
- selected layer names;
- selected dimensions;
- selected Dirichlet weights;
- weighting candidate ID;
- selected `k`;
- validation macro-F1;
- test accuracy;
- test macro-F1;
- test balanced accuracy.

Context result:

- selected layers: `layer3`, `layer4`, `layer2`;
- weights: `0.4447430074`, `0.0284117237`, `0.5268452764`;
- weighting: `dirichlet_00`;
- `k=1`;
- test macro-F1: `0.9482433542`.

This equivalence check does not replace the full 45-context audit. The full
audit is recorded in `docs/FINAL_GITHUB_AUDIT.md`.
